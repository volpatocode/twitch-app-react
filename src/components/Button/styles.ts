import styled from "styled-components"

export const Button = styled.div`
     a{
        cursor: pointer;
        color:#EFEFF1;
        padding:10px;
        text-align: center;
        width: fit-content;
        font-size: 1.5rem;
        font-weight: 600;
    }

    a:hover{
        color:#a970ff;
    }
`
   