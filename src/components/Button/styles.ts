import styled from "styled-components"

export const Button = styled.div`
    color:#f6f6f6;
    padding:10px;
    text-align: center;
    width: fit-content;
    font-size: 2rem;
    font-weight: 600;

    a{
        cursor: pointer;
    }
`