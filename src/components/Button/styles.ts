import styled from "styled-components";

export const Button = styled.a`
  cursor: pointer;
  color: #efeff1;
  padding: 10px;
  text-align: center;
  width: fit-content;
  font-size: 1.7rem;
  font-weight: 600;

  :hover {
    color: #a970ff;
  }
`;
