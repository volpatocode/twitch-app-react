import styled from "styled-components"

export const TripleDotIcon= styled.div`
    button{
        padding: 1px;
        cursor: pointer;
        background-color: transparent;
        max-height: fit-content;
    }


    svg:hover{
    background-color: rgba(0,0,0,0.3);
  }
`