import styled from "styled-components"

export const BadgeSpec = styled.div`
    color:#f6f6f6;
    background:rgba(0,0,0,0.6);
    padding:2px;
    text-align: center;
    width: fit-content;
    font-size: 0.8rem;
`