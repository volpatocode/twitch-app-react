import styled from "styled-components"
import { propsType } from "./index"


export const IconChest = styled.div<propsType>`
    button{
        cursor: pointer;
        background: none;
        padding: 3px;
    }

    button:hover{
        background:hsla(0,0%,20%,075);
    }
`

